/* 
 * The files included here are created using spinnaker and spinc
 * (see the Makefile for full details): 
 *
 *    cp darray.p1 darray.h
 *
 *    spinnaker -p -a -I  Catalina_LMM_dynamic.spin -o lmmd
 *    spinc lmmd.binary > lmmd.inc
 *
 *    spinnaker -p -a -I  Catalina_CMM_dynamic -o cmmd
 *    spinc cmmd.binary > cmmd.inc
 *
 */
#if defined(__CATALINA_COMPACT)

// cmmd kernel goes here ...

#include "cmmd.inc"

#else

// lmmd kernel goes here ...

#include "lmmd.inc"

#endif
