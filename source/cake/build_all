#!/bin/bash
##!/bin/bash

cd src
gcc build.c -D__CATALINA__ -o build
./build
cp cake $LCCDIR/bin/cake
cp cakeconf.h $LCCDIR/bin/cakeconf.h
cd ..

if [[ "$1" != "" ]]; then

   # also build cake for the specified Propeller platform
   make_cake $@

fi

